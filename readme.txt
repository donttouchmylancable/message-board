This is just a basic message-board.The messages are stored in a array.
I might change that in the future, so the messages are being stored in a database.

When restarting all messages get deleted, except the example Message.
